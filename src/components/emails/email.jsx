export default function Email({ url }) {
	return (
		<section>
			<button
				href="https://example.com"
				style={{
					background: "#000",
					color: "#fff",
					padding: "12px 20px",
				}}
			>
				Pergi ke halaman
			</button>
		</section>
	);
}
